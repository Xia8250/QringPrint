# QringPrint v4.1.0 — 广告横带

发布日期：2026-09-11

## ✨ 新功能

### 🎯 广告横带模式
新增**完全独立的固定模板打印模式**，与其他文字打印模式完全分离：

- **固定模板**：字号、字体、粗细、排版、对齐全部锁死，用户无法修改
- **加粗强制**：使用粗体字打印，无法关闭加粗
- **字号自动最大化**：根据纸宽（384 点）和文字长度自动计算最大字号
- **整条单行**：所有换行被自动合并为一条连续文字
- **整体顺时针旋转 90°**：将整条文字作为一个整体旋转，不是逐字逐行
- **文字垂直居中**：横带在纸宽方向上自动居中，不再顶部贴边
- **所见即所打**：预览和打印共用同一份光栅数据，绝无偏差
- **预览 = 硬阈值二值化**：预览直接显示 1-bit 二值图，与打印机实际吐纸完全一致

适用场景：店铺招牌、商品标签、广告条幅、货架标识等需要单行大号粗体文字的场景。

## 🐛 修复

- 修复了「文字已经打印完成但机器仍在走纸」的问题，将出纸余量从 360 点（45 mm）调整为 24 点（3 mm），刚好让最后一行离开打印头可干净撕纸
- 修复了预览和打印效果不一致的问题（预览走抗锯齿原图、打印机走硬阈值二值化），现在两者完全一致
- 修复了老版本「文字打印」页中「广告横带」模式出现多列错位的问题（已彻底移除该模式，统一使用新的独立页面）

## 📦 安装

1. 下载底部的 `app-release.apk`
2. 在 Android 手机的「设置 → 安全 → 安装未知来源」中允许安装
3. 打开 APK 文件安装即可
4. 首次启动需要授予蓝牙/位置/存储权限

## ⚙️ 技术信息

- **最低系统**：Android 8.0 (API 26)
- **目标系统**：Android 14 (API 34)
- **应用包名**：`huanxongkuaiyin.com`
- **版本号**：4.1.0 (versionCode 41)
- **签名**：使用项目自有 release.keystore 签名
- **大小**：约 266 MB（包含 ONNX Runtime、OpenCV、Tesseract 本地库，支持离线 OCR）

## 🔐 权限说明

| 权限 | 用途 |
|---|---|
| 蓝牙 / 蓝牙扫描 | 连接热敏打印机 |
| 位置 | Android 蓝牙扫描需要（系统策略） |
| 存储 / 媒体 | 读取相册图片进行打印 |
| 相机 | 拍照打印、OCR 拍照转文字 |

## 🔐 完整性校验

```
SHA-256: 8FA3E02F33B69E17943BC387834F96575A1188B32F139DE6373491FA2F1F006E
大小:    254.39 MB (266,751,801 字节)
```

校验命令（Windows PowerShell）：
```powershell
Get-FileHash .\app-release.apk -Algorithm SHA256
```

校验命令（macOS / Linux）：
```bash
shasum -a 256 app-release.apk
```

## 📞 反馈

如有问题或建议，请到 [Issues](https://github.com/Xia8250/huanxongkuaiyin-updates/issues) 提交。
